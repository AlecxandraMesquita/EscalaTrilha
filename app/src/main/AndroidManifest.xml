<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.alecxandramesquita.escala">

    <application
        android:allowBackup="true"
        android:icon="@drawable/centopeia"
        android:label="@string/app_name"
        android:roundIcon="@drawable/centopeia"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LayoutTelaLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LayoutTelaSobre"/>
        <activity android:name=".LayoutTelaPerfil" />
        <activity android:name=".TelaEquipes" />
        <activity android:name=".LayoutTelaEscala" />
        <activity android:name=".LayoutTelaVoluntarios" />

    </application>

</manifest>